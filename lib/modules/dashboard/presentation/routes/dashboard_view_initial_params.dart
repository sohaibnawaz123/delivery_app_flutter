class DashboardViewInitialParams {
  const DashboardViewInitialParams();

  factory DashboardViewInitialParams.fromUri(Uri uri) {
    return DashboardViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}
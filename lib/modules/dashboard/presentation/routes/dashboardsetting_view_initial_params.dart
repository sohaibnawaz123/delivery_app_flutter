class DashboardsettingViewInitialParams {
  const DashboardsettingViewInitialParams();

  factory DashboardsettingViewInitialParams.fromUri(Uri uri) {
    return DashboardsettingViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}
class PrivacysettingViewInitialParams {
  const PrivacysettingViewInitialParams();

  factory PrivacysettingViewInitialParams.fromUri(Uri uri) {
    return PrivacysettingViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}
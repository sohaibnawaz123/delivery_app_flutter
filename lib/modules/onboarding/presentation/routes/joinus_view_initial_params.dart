class JoinusViewInitialParams {
  const JoinusViewInitialParams();

  factory JoinusViewInitialParams.fromUri(Uri uri) {
    return JoinusViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}
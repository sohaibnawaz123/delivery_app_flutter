class ProductdetailViewInitialParams {
  const ProductdetailViewInitialParams();

  factory ProductdetailViewInitialParams.fromUri(Uri uri) {
    return ProductdetailViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}